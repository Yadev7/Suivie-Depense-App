export const environment = {
/* The code snippet is defining an object named `environment` with two properties: `production` and
`api`. */
    // production: true,
    // api: 'http://localhost:3002'
  };
