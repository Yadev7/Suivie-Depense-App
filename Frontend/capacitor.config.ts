import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.suividep.app',
  appName: 'suivi-depense',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
